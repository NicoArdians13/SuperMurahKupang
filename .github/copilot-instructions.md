# Project Instructions

- This is a Vite React TypeScript marketing website for an Indonesian wholesale household furniture store.
- Keep the interface responsive, accessible, and focused on product discovery and WhatsApp ordering.
- Use existing project patterns and validate changes with `npm run build`.
- Keep external image URLs as replaceable product placeholders until the store provides its own photography.
